#ifndef BUILD_VERSION_H
#define BUILD_VERSION_H
/* Noncanonical/IDE fallback. Canonical builds generate this header first in .build.
 * It must never impersonate a released firmware number. */
#define EBICS_BUILD_VERSION "DEV-NONCANONICAL"
#endif
